<%- include(process.cwd() + '/app/partials/section-sub-header', {headline: 'Jump to:'}) %>
<div class="row">
  <div class="jump-to col-md-10 col-md-offset-1">
    <% sections.forEach(function(section) { %>
      <a href="<%= section.link %>">
        <%= section.name %>
      </a>
    <% }) %>
  </div>
</div>